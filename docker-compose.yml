version: '3'

services:
  hoshikumo-bot:
    build: .
    image: hoshikumo-bot
    container_name: 'hoshikumo-bot'
    ports:
      - '1551:1551'
    volumes:
      - db:/src/db
      - log:/src/log
  redis:
    image: redis:alpine
    container_name: 'hoshikumo-redis'
    restart: always
    volumes:
        - redis-data:/data
volumes:
  db:
  log:
  redis-data:
